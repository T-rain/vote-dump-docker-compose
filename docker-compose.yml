version: '3.1'

services:
  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: your_password 
    volumes:
       - /your_location/local_db.dump:/local_db.dump
       - /your_location/scripts/:/scripts
